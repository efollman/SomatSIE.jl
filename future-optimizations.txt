there are two major optimization to be had with the library with there own problems with implementing.

1. Almost always the data in the SIE file is stored as Float32, however libsie always pulls them as Float64 values,
the main issue here is properly inplementing in the zig lib without breaking anyting
not sure how the Cabi will change if it can vary between Float32 or Float64.
cant rely on the tags, would have to pull the type info from the decoder, not sure how difficult this will be.

2. The other main thing is in 90% of cases the channel is time series and the time vec is well defined.
the main issue here is dealing with edge cases and ensuring it is valid to do so,
but the time vector could be inferred to a LinRange, completley bypassing the need to read it.
for now stick to somat:sequential and datamode_type time_history to avoid these issues and handle most cases.
Im pretty confident that will work, it will be harder to expand this idea to other cases if it is desired in the future,
probably not worth it in that case.

With these optimizations in over 90% of cases we could cut both read performance by 50%+ and memory usage by 75%

Currently im holding off on this as it deviates from how the original libsie functions,
and slightly more than trivial to implement (really shouldnt be too hard though in theory)