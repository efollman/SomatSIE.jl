Change types from AbstratTest,AbstratChannel,AbstractDimension to Test,Channel,Dimension ect, While reading from file those variant types should be FileTest,FileChannel,FileDimension

dimension should no longer behave as a dimension, rather it should have the propery .vec which will return the proper vector type for the dimension, for in memory version this vector should be settable (i.e. dim.vec .*= 2 should edit the vector held in the property) with file type trying to change the vector directly should error

remove caching as well as currently it is too confusing what is being held in memory and when. however keep the ability to pull partial vecs with index efficiently with in file types.

findchannel currently only works with Test type, it should also work with vector of channels type.

findchannel should also work with Int/UInt input pulling off id number rather than channel name as well.

?? fix indexing with channel vecs to be from id (sparce Vecor may make this not horrible, however it might make things too complicated in general to work with or implement.)