abstract vector math ops need to return original type if possible, 
(currently dereferences to regular Vector) 
+ maybe uneeded with this implemented but shorcut constructor that pulls props from already existing object and vector to create new could be useful.

should channel be subtype of AbstractMatrix? 
would cause issues possibly with raw data and channel types that dont have similar vector lengths 
e.g. histogram. does AbstractMatrix handle sparse cases, does something else handle this?

should remove detachsie and add method to base read function.
remove caching as well as currently it is too confusing what is being held in memory and when.
with this if possible have optional argument to read partial vector (index input)